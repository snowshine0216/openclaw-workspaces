# Session: 2026-02-22 04:03:56 UTC

- **Session Key**: agent:information-collector:main
- **Session ID**: d032fb0d-5a3a-4e9c-967b-8a618f525066
- **Source**: gateway:sessions.reset

## Conversation Summary

user: Conversation info (untrusted metadata):
```json
{
  "message_id": "32c9d7bd-b1a0-475c-a032-4a3b604dda11",
  "sender_id": "gateway-client",
  "sender": "gateway-client"
}
```

[Sun 2026-02-22 03:39 UTC] now read https://github.com/PleasePrompto/notebooklm-mcp carefully and i would like to istall noteboolkm mcp so that my openclaw can read it, give me your plan first
assistant: Okay, I've done a thorough investigation. Here's my plan:

---

## 🔭 NotebookLM MCP for OpenClaw — The Plan

### What NotebookLM MCP Does
It's an MCP server that lets AI agents query your NotebookLM notebooks directly via Chrome browser automation. You upload docs to NotebookLM, and then the MCP server automates a Chrome session to ask questions and get zero-hallucination, citation-backed answers from Gemini.

### The Problem: OpenClaw Doesn't Natively Support MCP
OpenClaw isn't Claude Code / Codex / Cursor — it doesn't have a built-in `mcp add` command. There's no MCP client integration in OpenClaw's config. So we can't just plug it in directly.

### 🚨 Bigger Problem: Browser Automation on a Headless VPS
NotebookLM MCP works by **automating Chrome to interact with notebooklm.google.com**. Your server:
- ✅ Has Puppeteer's Chrome cached at `/root/.cache/puppeteer/chrome/`
- ❌ No display server (headless VPS) — NotebookLM requires **Google login** via a visible Chrome window
- ❌ Auth flow requires you to visually log into Google in a browser

### Possible Approaches

**Option A: Wrap it as an OpenClaw Skill (recommended if it works)**
1. Install `notebooklm-mcp` globally via npm
2. Write a skill that spawns the MCP server as a subprocess and communicates with it via stdio (MCP protocol)
3. The skill would translate between OpenClaw tool calls and MCP messages
4. **Blocker**: Still needs Chrome with Google auth — would need to either:
   - Forward a display via VNC/X11 for the one-time auth
   - Or do auth on your local machine and copy the Chrome profile to the server

**Option B: Run it locally on your machine, not on the VPS**
- Install on your local machine where you have a real browser
- Use it from Claude Code / Cursor locally
- OpenClaw stays separate

**Option C: Hybrid — use NotebookLM API directly (if one exists)**
- NotebookLM doesn't have a public API. The MCP server reverse-engineers it via browser automation. No shortcut here.

### My Honest Assessment

This MCP is designed for **local dev tools** (Claude Code, Cursor) running on machines with a real browser and display. Running it on a headless VPS is fighting upstream. The core value — zero-hallucination answers from your docs — is great, but the delivery mechanism (Chrome automation of a Google web app) is awkward for a server environment.

### What I'd Recommend

1. **If you want it on this server**: I can install the npm package, write a skill wrapper, and we set up VNC or `xvfb` + a forwarded browser for the one-time Google auth. After auth, it should run headless with the saved session. This is doable but finicky.

2. **If you want it on your local machine**: Much simpler — install locally, use with Claude Code or any MCP client there.

3. **Alternative approach**: If your goal is "let Scout query a knowledge base without hallucinations," we could build something similar using Google's Gemini API directly with grounding, or use NotebookLM's share links + web scraping as a lighter approach.

---

**What's your call, Snow?** Want me to go with Option A (install on server + skill wrapper + headless Chrome setup), or do you prefer a different path?
