# Session: 2026-03-06 06:56:37 UTC

- **Session Key**: agent:main:main
- **Session ID**: eef25773-2d7e-4104-87a3-cb053681f07a
- **Source**: feishu

## Conversation Summary

user: Conversation info (untrusted metadata):
```json
{
  "message_id": "a7ea00b0-e288-48d4-b866-66a3006e6b29",
  "sender_id": "gateway-client",
  "sender": "openclaw-tui"
}
```

Sender (untrusted metadata):
```json
{
  "label": "openclaw-tui (gateway-client)",
  "id": "gateway-client",
  "name": "openclaw-tui",
  "username": "openclaw-tui"
}
```

[Thu 2026-03-05 23:55 UTC] now discuss with me how i can setup rules to let the 3 agents to write clean. functional modular codes with tdd design (always have tests and strcutured layers / folders?) do i have to update agents.md for each or using skills? you can user clawddocs
assistant: I’ll pull up the local `clawddocs` skill, then outline the clean-code/TDD rules and where to enforce them (AGENTS.md vs skills vs per-agent config).
assistant: You don’t “update AGENTS.md for each agent” as the main mechanism. Use both, but for different scopes:

- **Skills** = cross-project, agent-behavior rules (“write modular code”, “TDD first”, “always add tests”, “layering conventions”).
- **`AGENTS.md` in a repo** = project-specific coding conventions (folder layout, naming, how to run tests, architectural boundaries). Any coding agent operating in that repo will follow it.

From OpenClaw’s docs: in multi-agent setups each agent has its own workspace; **shared skills** live in `~/.openclaw/skills` and apply to all agents, while **per-agent skills** live in that agent’s `<workspace>/skills` (workspace overrides shared) (`/root/.nvm/versions/node/v25.6.1/lib/node_modules/openclaw/docs/tools/skills.md:23`).

**What I’d do (clean + enforceable)**

1) Put your “engineering constitution” into a shared skill  
- Create `~/.openclaw/skills/clean-tdd/SKILL.md` with rules like:
  - Always start with a failing test for bug/feature (red → green → refactor).
  - No giant files; max one responsibility per module.
  - Pure functions + dependency injection; avoid hidden globals.
  - Tests required for every change; prefer unit tests, add integration tests for boundaries.
  - Require running `test` and `lint` commands before finalizing.
- Benefit: all 3 agents inherit the same standards everywhere.

2) Put “this project’s actual layout” into `projects/<name>/AGENTS.md`  
In the repo you’re coding in, add an `AGENTS.md` (or extend an existing one) that answers:
- Canonical structure (example):
  - `src/domain/` (pure business logic)
  - `src/application/` (use-cases/services)
  - `src/adapters/` (db/http/fs)
  - `src/entrypoints/` (api/cli)
  - `tests/unit/`, `tests/integration/`
- Naming rules, dependency direction (domain must not import adapters), and exact commands:
  - `pnpm test`, `pnpm lint`, `pnpm typecheck` (whatever your stack is)
- Benefit: agents don’t guess; they comply with *this* codebase.

3) Use per-agent skills only when an agent has a special role  
Example: “Agent A = refactorer”, “Agent B = test-writer”, “Agent C = reviewer”.  
Then you’d put extra role-specific guidance into that agent’s `<workspace>/skills/...`.

4) Optional but powerful: enforce via CI  
Prompt rules help, but **CI is the only real enforcement**. Add a pipeline that fails if:
- tests not present / not run
- coverage drops below threshold (if you want)
- lint/typecheck fail

If you tell me your stack (Python/TS/Go/etc) and where the 3 agents live (`agents.list` in `openclaw.json`), I can draft:
- A reusable `clean-tdd` skill
- A template `AGENTS.md` for your project
- A minimal CI config that enforces “no test, no merge”
assistant: [{model} | {think}] I’ll pull up the local `clawddocs` skill, then outline the clean-code/TDD rules and where to enforce them (AGENTS.md vs skills vs per-agent config).
assistant: [{model} | {think}] You don’t “update AGENTS.md for each agent” as the main mechanism. Use both, but for different scopes:

- **Skills** = cross-project, agent-behavior rules (“write modular code”, “TDD first”, “always add tests”, “layering conventions”).
- **`AGENTS.md` in a repo** = project-specific coding conventions (folder layout, naming, how to run tests, architectural boundaries). Any coding agent operating in that repo will follow it.

From OpenClaw’s docs: in multi-agent setups each agent has its own workspace; **shared skills** live in `~/.openclaw/skills` and apply to all agents, while **per-agent skills** live in that agent’s `<workspace>/skills` (workspace overrides shared) (`/root/.nvm/versions/node/v25.6.1/lib/node_modules/openclaw/docs/tools/skills.md:23`).

**What I’d do (clean + enforceable)**

1) Put your “engineering constitution” into a shared skill  
- Create `~/.openclaw/skills/clean-tdd/SKILL.md` with rules like:
  - Always start with a failing test for bug/feature (red → green → refactor).
  - No giant files; max one responsibility per module.
  - Pure functions + dependency injection; avoid hidden globals.
  - Tests required for every change; prefer unit tests, add integration tests for boundaries.
  - Require running `test` and `lint` commands before finalizing.
- Benefit: all 3 agents inherit the same standards everywhere.

2) Put “this project’s actual layout” into `projects/<name>/AGENTS.md`  
In the repo you’re coding in, add an `AGENTS.md` (or extend an existing one) that answers:
- Canonical structure (example):
  - `src/domain/` (pure business logic)
  - `src/application/` (use-cases/services)
  - `src/adapters/` (db/http/fs)
  - `src/entrypoints/` (api/cli)
  - `tests/unit/`, `tests/integration/`
- Naming rules, dependency direction (domain must not import adapters), and exact commands:
  - `pnpm test`, `pnpm lint`, `pnpm typecheck` (whatever your stack is)
- Benefit: agents don’t guess; they comply with *this* codebase.

3) Use per-agent skills only when an agent has a special role  
Example: “Agent A = refactorer”, “Agent B = test-writer”, “Agent C = reviewer”.  
Then you’d put extra role-specific guidance into that agent’s `<workspace>/skills/...`.

4) Optional but powerful: enforce via CI  
Prompt rules help, but **CI is the only real enforcement**. Add a pipeline that fails if:
- tests not present / not run
- coverage drops below threshold (if you want)
- lint/typecheck fail

If you tell me your stack (Python/TS/Go/etc) and where the 3 agents live (`agents.list` in `openclaw.json`), I can draft:
- A reusable `clean-tdd` skill
- A template `AGENTS.md` for your project
- A minimal CI config that enforces “no test, no merge”
